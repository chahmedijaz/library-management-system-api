import { Document } from "mongoose";
import { SessionStorage } from "../entities";

export class BaseController {
    // constructor(
    //     public sessionId: string,
    //     public currentUser: any = (SessionStorage.sessionUser(sessionId)),
    //     public currentLibrary: any = SessionStorage.sessionLibrary(sessionId),
    // ) { }
};
